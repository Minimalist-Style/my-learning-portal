// =====================================================
// 🎓 УРОК JAVASCRIPT: TO-DO LIST
// =====================================================
// 
// В этом файле мы изучим:
// 1. Переменные (let, const)
// 2. Массивы (хранение списка задач)
// 3. Функции (действия)
// 4. События (реакция на клики)
// 5. Работа с DOM (изменение страницы)
// =====================================================

// ===== ШАГ 1: НАХОДИМ ЭЛЕМЕНТЫ НА СТРАНИЦЕ =====
// 
// document.getElementById('id') — находит элемент по его id
// Сохраняем в константы (const) — они не будут меняться

const todoForm = document.getElementById('todoForm');       // Форма
const todoInput = document.getElementById('todoInput');     // Поле ввода
const todoList = document.getElementById('todoList');       // Список <ul>
const totalCount = document.getElementById('totalCount');   // Счётчик всего
const doneCount = document.getElementById('doneCount');     // Счётчик выполненных
const emptyMessage = document.getElementById('emptyMessage'); // Сообщение "список пуст"


// ===== ШАГ 2: МАССИВ ДЛЯ ХРАНЕНИЯ ЗАДАЧ =====
// 
// Массив — это список элементов: [элемент1, элемент2, ...]
// let — переменная, которая может меняться

let todos = [];
// Каждая задача будет объектом: { id: 1, text: "Купить молоко", done: false }


// ===== ШАГ 3: ФУНКЦИЯ ОТРИСОВКИ СПИСКА =====
// 
// Эта функция берёт массив todos и создаёт HTML для каждой задачи
// Вызывается каждый раз когда список меняется

function renderTodos() {
    // Очищаем список
    todoList.innerHTML = '';

    // Проходим по каждой задаче в массиве
    // forEach — делает что-то для КАЖДОГО элемента массива
    todos.forEach(function (todo) {
        // Создаём элемент <li>
        const li = document.createElement('li');
        li.className = 'todo-item';

        // Внутренний HTML элемента
        // Обрати внимание на onclick="..." — это обработчики событий
        li.innerHTML = `
            <div class="todo-checkbox ${todo.done ? 'checked' : ''}" 
                 onclick="toggleTodo(${todo.id})">
            </div>
            <span class="todo-text ${todo.done ? 'completed' : ''}">
                ${todo.text}
            </span>
            <button class="todo-delete" onclick="deleteTodo(${todo.id})">
                ×
            </button>
        `;

        // Добавляем <li> в список <ul>
        todoList.appendChild(li);
    });

    // Обновляем счётчики и сообщение
    updateStats();
}


// ===== ШАГ 4: ФУНКЦИЯ ДОБАВЛЕНИЯ ЗАДАЧИ =====

function addTodo(text) {
    // Создаём объект задачи
    const newTodo = {
        id: Date.now(),  // Уникальный ID (текущее время в миллисекундах)
        text: text,      // Текст задачи
        done: false      // Выполнена? Нет
    };

    // Добавляем в массив
    // push() — добавляет элемент в конец массива
    todos.push(newTodo);

    // Перерисовываем список
    renderTodos();

    // Выводим в консоль для отладки
    console.log('Добавлена задача:', newTodo);
    console.log('Все задачи:', todos);
}


// ===== ШАГ 5: ФУНКЦИЯ ПЕРЕКЛЮЧЕНИЯ СТАТУСА =====

function toggleTodo(id) {
    // Находим задачу по id
    // find() — ищет элемент в массиве по условию
    const todo = todos.find(function (t) {
        return t.id === id;
    });

    // Если нашли — переключаем статус
    if (todo) {
        todo.done = !todo.done;  // ! — оператор НЕ (превращает true в false и наоборот)
        renderTodos();
        console.log('Переключена задача:', todo);
    }
}


// ===== ШАГ 6: ФУНКЦИЯ УДАЛЕНИЯ ЗАДАЧИ =====

function deleteTodo(id) {
    // filter() — создаёт новый массив, оставляя только элементы, 
    // для которых функция вернула true
    todos = todos.filter(function (t) {
        return t.id !== id;  // Оставляем все, кроме удаляемой
    });

    renderTodos();
    console.log('Удалена задача с id:', id);
}


// ===== ШАГ 7: ФУНКЦИЯ ОБНОВЛЕНИЯ СТАТИСТИКИ =====

function updateStats() {
    // Общее количество
    totalCount.textContent = todos.length;

    // Количество выполненных
    // filter() + length — считаем сколько done === true
    const completed = todos.filter(function (t) {
        return t.done === true;
    });
    doneCount.textContent = completed.length;

    // Показываем/скрываем сообщение о пустом списке
    if (todos.length === 0) {
        emptyMessage.classList.remove('hidden');
    } else {
        emptyMessage.classList.add('hidden');
    }
}


// ===== ШАГ 8: ОБРАБОТЧИК ОТПРАВКИ ФОРМЫ =====
// 
// addEventListener — "слушает" события
// 'submit' — событие отправки формы (нажатие Enter или кнопки)

todoForm.addEventListener('submit', function (event) {
    // Предотвращаем перезагрузку страницы
    // (по умолчанию форма перезагружает страницу)
    event.preventDefault();

    // Получаем текст из поля ввода
    // .value — значение поля
    // .trim() — убирает пробелы по краям
    const text = todoInput.value.trim();

    // Если текст не пустой — добавляем задачу
    if (text !== '') {
        addTodo(text);
        todoInput.value = '';  // Очищаем поле ввода
        todoInput.focus();     // Ставим курсор обратно в поле
    }
});


// ===== ШАГ 9: НАЧАЛЬНАЯ ОТРИСОВКА =====

renderTodos();


// =====================================================
// 🎓 ШПАРГАЛКА ПО JAVASCRIPT:
// =====================================================
//
// ПЕРЕМЕННЫЕ:
//   let name = 'значение';    // Можно менять
//   const name = 'значение';  // Нельзя менять
//
// МАССИВЫ:
//   let arr = [1, 2, 3];      // Создание
//   arr.push(4);              // Добавить в конец
//   arr.length;               // Длина массива
//   arr.forEach(fn);          // Для каждого элемента
//   arr.filter(fn);           // Отфильтровать
//   arr.find(fn);             // Найти один элемент
//
// ФУНКЦИИ:
//   function name(arg) { ... }           // Обычная
//   const name = function(arg) { ... }   // Как переменная
//
// DOM:
//   document.getElementById('id')        // Найти по id
//   document.createElement('tag')        // Создать элемент
//   element.innerHTML = '...'            // Изменить HTML внутри
//   element.textContent = '...'          // Изменить текст
//   element.appendChild(child)           // Добавить дочерний элемент
//   element.classList.add('class')       // Добавить класс
//   element.classList.remove('class')    // Убрать класс
//
// СОБЫТИЯ:
//   element.addEventListener('click', function() { ... })
//   event.preventDefault()               // Отменить стандартное действие
//
// =====================================================
