# 📝 Конспект обучения

## Урок 1 — 16 декабря 2025

### Что изучили:

#### 1. Инструменты разработчика
| Инструмент | Для чего |
|------------|----------|
| **Node.js** | Запускает JavaScript на компьютере |
| **npm** | "Магазин" библиотек для JavaScript |
| **npx** | Запускает программы из npm |
| **Git** | Сохраняет версии кода |
| **Python** | Другой язык программирования |
| **pip** | "Магазин" для Python |
| **VS Code** | Редактор кода |
| **Терминал** | Текстовое управление компьютером |

#### 2. Структура веб-проекта
```
Проект/
├── index.html    ← структура (ЧТО на странице)
├── style.css     ← стили (КАК выглядит)
├── script.js     ← логика (КАК реагирует)
└── package.json  ← "паспорт" проекта
```

#### 3. Команды терминала
```bash
ls                 # показать файлы в папке
npm init -y        # создать package.json
npx serve .        # запустить веб-сервер
npm install        # установить зависимости
```

#### 4. HTML теги
- `<div>` — блок/контейнер
- `<h1>` — заголовок
- `<p>` — параграф
- `<button>` — кнопка
- `<span>` — inline элемент

#### 5. CSS свойства
- `background: linear-gradient()` — градиент
- `border-radius` — скругление
- `box-shadow` — тень
- `display: flex` — расположение элементов
- `transition` — плавная анимация
- `@keyframes` — создание анимации

#### 6. JavaScript основы
- `let` — создать переменную
- `document.getElementById()` — найти элемент
- `addEventListener('click', func)` — реакция на клик
- `textContent` — изменить текст

---

### Создали проект:
**Интерактивная визитка** с:
- Красивым градиентным фоном
- Glassmorphism эффектом (стеклянная карточка)
- Счётчиком кликов
- Анимациями при наведении
- Меняющимися эмодзи (💪→🔥→⭐→🏆)

Запуск: `npx serve .` → открыть http://localhost:3000

---

### Следующие шаги:
См. файл `ПЛАН_ОБУЧЕНИЯ.md`

---

*Этот конспект автоматически сохранён в папке проекта*
